import airMode from './airMode.svelte';

export const SummernoteAirModeOption = () => {
    return {
      Component: airMode,
    }
  }
  
  SummernoteAirModeOption.story = {
    name: 'airMode',
  }